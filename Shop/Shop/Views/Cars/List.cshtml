<h1> Все автомобили </h1>
<h3>@Model.currCategory</h3>
<div class="row mt-5 mb-2">
    @{ 
        foreach (Shop.Date.Models.Car car in Model.allCars)
        {
          @Html.Partial("AllCars", car)  
        }
     }
</div>